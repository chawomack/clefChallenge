import React from 'react';
import './Note.css';


const Note = () => (
    <div className="Note"/>
);

const NoteKey = (note: string) => (
    <div className="NoteKey" onClick={() => {}}/>
);

export  {Note, NoteKey };